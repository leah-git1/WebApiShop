version: '3.8'

services:
  redis_cache:
    image: redis:7-alpine
    container_name: shop_redis
    ports:
      - "6379:6379"
    command: redis-server --requirepass Le114565
    restart: always

networks:
  default:
    name: webapi-shop-network